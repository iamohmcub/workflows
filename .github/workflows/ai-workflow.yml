name: AI Workflow Validate

on:
  push:
    branches:
      - main
    paths:
      - AGENTS.md
      - COMMANDS.md
      - DEMO.md
      - package.json
      - scripts/**
      - .ai/**
      - .github/workflows/ai-workflow.yml
  pull_request:
    branches:
      - main
    paths:
      - AGENTS.md
      - COMMANDS.md
      - DEMO.md
      - package.json
      - scripts/**
      - .ai/**
      - .github/workflows/ai-workflow.yml
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Validate AI workflow
        run: npm run ai:validate
